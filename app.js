const dialog=document.querySelector('#request-dialog');document.querySelectorAll('.open-request').forEach(button=>button.addEventListener('click',()=>dialog.showModal()));document.querySelector('.close').addEventListener('click',()=>dialog.close());document.querySelector('#request-form').addEventListener('submit',event=>{event.preventDefault();event.currentTarget.hidden=true;document.querySelector('#success').hidden=false});document.querySelector('#service-search').addEventListener('input',event=>{const text=event.target.value.toLowerCase();document.querySelectorAll('.category').forEach(item=>item.style.display=item.textContent.toLowerCase().includes(text)?'flex':'none')});document.querySelectorAll('.category').forEach(item=>item.addEventListener('click',()=>{document.querySelector('.category.active').classList.remove('active');item.classList.add('active')}));
