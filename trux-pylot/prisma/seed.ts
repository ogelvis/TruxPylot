import { PrismaClient, Role, VerificationStatus } from '@prisma/client';
import bcrypt from 'bcryptjs';
const prisma=new PrismaClient();
async function main(){for(const name of ['Electrical','Plumbing','AC & Cooling','Cleaning','Carpentry','Solar & Generator'])await prisma.serviceCategory.upsert({where:{slug:name.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/-$/,'')},update:{},create:{name,slug:name.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/-$/,'')}});const passwordHash=await bcrypt.hash('ChangeThisDemoPassword123!',12);const user=await prisma.user.upsert({where:{email:'pro.demo@truxpylot.test'},update:{},create:{email:'pro.demo@truxpylot.test',passwordHash,role:Role.PROFESSIONAL,professional:{create:{fullName:'Alex Okafor',profession:'Electrician',location:'Lekki, Lagos',verificationStatus:VerificationStatus.APPROVED,rating:4.9,completedJobs:128,wallet:{create:{availableBalance:0,pendingBalance:0}}}}}});const professional=await prisma.professional.findUniqueOrThrow({where:{userId:user.id}});const electrical=await prisma.serviceCategory.findUniqueOrThrow({where:{slug:'electrical'}});await prisma.professionalService.upsert({where:{professionalId_categoryId:{professionalId:professional.id,categoryId:electrical.id}},update:{},create:{professionalId:professional.id,categoryId:electrical.id,startingPrice:15000}})}main().finally(()=>prisma.$disconnect());
