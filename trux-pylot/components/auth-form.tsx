'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export function AuthForm({mode}:{mode:'login'|'register'}) { const router=useRouter(); const [error,setError]=useState(''); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const body=Object.fromEntries(f);const r=await fetch(`/api/auth/${mode}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)return setError(d.error||'Something went wrong');router.push(d.redirect||'/login');router.refresh()} return <form onSubmit={submit}><h1>{mode==='login'?'Welcome back':'Create your account'}</h1>{mode==='register'&&<><input name="fullName" placeholder="Full name" required/><select name="role" defaultValue="CUSTOMER"><option value="CUSTOMER">I need a professional</option><option value="PROFESSIONAL">I am a professional</option></select><input name="location" placeholder="Location" required/></>}<input name="email" type="email" placeholder="Email address" required/><input name="password" type="password" minLength={12} placeholder="Password (12+ characters)" required/><button type="submit">{mode==='login'?'Sign in':'Create account'}</button>{error&&<p role="alert">{error}</p>}</form> }
