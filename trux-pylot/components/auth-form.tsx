'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export function AuthForm({mode}:{mode:'login'|'register'}) { const router=useRouter(); const [error,setError]=useState(''); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');const f=new FormData(e.currentTarget);const body=Object.fromEntries(f);const r=await fetch(`/api/auth/${mode}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json().catch(()=>({error:'The service is temporarily unavailable. Please try again.'}));if(!r.ok)return setError(d.error||'Something went wrong');router.push(d.redirect||'/login');router.refresh()} return <form className="auth-form" onSubmit={submit}><p className="eyebrow">{mode==='login'?'WELCOME BACK':'JOIN TRUX PYLOT'}</p><h1>{mode==='login'?'Sign in to your account':'Create your account'}</h1><p>{mode==='login'?'Manage jobs, messages and payments in one secure place.':'Find work or trusted help. It only takes a minute to begin.'}</p>{mode==='register'&&<><input name="fullName" placeholder="Full name" required/><select name="role" defaultValue="CUSTOMER"><option value="CUSTOMER">I need a professional</option><option value="PROFESSIONAL">I am a professional</option></select><input name="location" placeholder="Your location" required/></>}<input name="email" type="email" placeholder="Email address" required/><input name="password" type="password" minLength={12} placeholder="Password (12+ characters)" required/><button type="submit">{mode==='login'?'Sign in securely →':'Create account →'}</button>{error&&<p role="alert">{error}</p>}<p className="auth-switch">{mode==='login'?<>New to Trux Pylot? <a href="/register">Create an account</a></>:<>Already have an account? <a href="/login">Sign in</a></>}</p></form> }
