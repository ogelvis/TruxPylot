'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
export default function NgNjiAdminLogin(){const router=useRouter();const [email,setEmail]=useState(''),[code,setCode]=useState(''),[sent,setSent]=useState(false),[twoFA,setTwoFA]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');async function send(e?:React.FormEvent){e?.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/auth/otp/send',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mode:'admin-login',email})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Administrative sign in is unavailable.');setSent(true)}catch(e){setError(e instanceof Error?e.message:'Could not reach the server.')}finally{setBusy(false)}}async function verify(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await fetch(twoFA?'/api/auth/2fa/verify-login':'/api/auth/otp/verify',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,code})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Verification failed.');if(d.requires2FA){setTwoFA(true);setCode('');return}router.replace(d.redirect||'/dashboard/NgNji');router.refresh()}catch(e){setError(e instanceof Error?e.message:'Could not verify your access.')}finally{setBusy(false)}}return <main className="tp-admin-gate"><section className="tp-admin-card"><div className="tp-admin-mark">TP</div><p className="eyebrow">SECURE CONTROL CENTER</p><h1>TruxPylot Admin</h1><p>Authorized administrative access only. Email verification and 2-step authentication protect this area.</p>{!sent?<form onSubmit={send}><label>Verified admin email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="username"/></label><button disabled={busy}>{busy?'Checking…':'Continue securely →'}</button></form>:<form onSubmit={verify}><label>{twoFA?'Authenticator code':'Email verification code'}<input value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,'').slice(0,6))} inputMode="numeric" maxLength={6} placeholder="6-digit code" required autoComplete="one-time-code"/></label><button disabled={busy}>{busy?'Verifying…':twoFA?'Verify 2FA →':'Verify email →'}</button><button type="button" className="tp-admin-secondary" onClick={()=>{setSent(false);setTwoFA(false);setCode('');setError('')}}>Use another email</button></form>}{error&&<p className="tp-admin-error" role="alert">{error}</p>}</section></main>}
