import { requireRole } from '@/lib/guard';
import { prisma } from '@/lib/prisma';
import { AppShell } from '@/components/app-shell';
export default async function AdminDashboard(){await requireRole('ADMIN');const [customers,professionals,pending,jobs,payments]=await Promise.all([prisma.customer.count(),prisma.professional.count(),prisma.professional.count({where:{verificationStatus:{in:['SUBMITTED','UNDER_REVIEW']}}}),prisma.job.count(),prisma.payment.aggregate({_sum:{amount:true},where:{status:'SUCCESS'}})]);return <AppShell role="ADMIN" name="Platform admin"><main className="dash-page"><p className="page-kicker">ADMIN CONSOLE</p><h1>Platform performance at a glance.</h1><p className="subcopy">Monitor marketplace growth, trust operations and payments from one place.</p><section className="metrics"><div className="metric"><span>Total customers</span><b>{customers}</b><small>Registered accounts</small></div><div className="metric"><span>Professionals</span><b>{professionals}</b><small>{pending} awaiting review</small></div><div className="metric"><span>Total jobs</span><b>{jobs}</b><small>All service requests</small></div><div className="metric"><span>Transaction value</span><b>₦{((payments._sum.amount??0)/100).toLocaleString()}</b><small>Successful payments</small></div></section><section className="panel"><div className="panel-head"><h2>Trust operations</h2><a href="/dashboard/admin/verifications">Review verifications →</a></div><div className="empty">{pending?`${pending} professional verification request${pending===1?'':'s'} need review.`:'No verification reviews are waiting right now.'}</div></section></main></AppShell>}
