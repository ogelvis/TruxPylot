import { requireRole } from '@/lib/guard';
import { prisma } from '@/lib/prisma';
export default async function AdminDashboard(){await requireRole('ADMIN');const [customers,professionals,pending,jobs,payments]=await Promise.all([prisma.customer.count(),prisma.professional.count(),prisma.professional.count({where:{verificationStatus:{in:['SUBMITTED','UNDER_REVIEW']}}}),prisma.job.count(),prisma.payment.aggregate({_sum:{amount:true},where:{status:'SUCCESS'}})]);return <main><p>ADMIN DASHBOARD</p><h1>Platform overview</h1><section><article>Customers: {customers}</article><article>Professionals: {professionals}</article><article>Pending verifications: {pending}</article><article>Jobs: {jobs}</article><article>Transaction value: ₦{((payments._sum.amount??0)/100).toLocaleString()}</article></section><a href="/dashboard/admin/verifications">Review verifications →</a></main>}
