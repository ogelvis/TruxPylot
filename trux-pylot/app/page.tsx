import { prisma } from '@/lib/prisma';
export const dynamic = 'force-dynamic';
export default async function Home() { const categories = await prisma.serviceCategory.findMany({where:{active:true},take:12,orderBy:{name:'asc'}}); return <main><header><b>TRUX PYLOT</b><a href="/marketplace">Find a professional</a><a href="/register">Join as a professional</a></header><section><p>TRUSTED PROFESSIONALS, READY TO WORK</p><h1>Find the right professional for every job.</h1><p>Book dependable, verified experts for your home, business or estate.</p><a href="/marketplace">Find a professional →</a></section><section><h2>Explore services</h2>{categories.map(c=><a key={c.id} href={`/marketplace?category=${c.slug}`}>{c.name}</a>)}</section></main>; }
